@model Panasia.Gemini.Web.Models.BaseModels.PageModelBase
@{
    ViewBag.Title = "_ToolsBar";
}

@using Panasia.Gemini

@if (Model != null)
{
    <input id="hiddenForController" hidden type="text" value="@Model.AddLink.LinkPage.ControllerName"></input>
    <input id="hiddenForcAutoKey" hidden type="text" value=""></input>
        <div class="top-nav">
            @if (Model.AddLink.BindingEnable)
            {
                <a href="@Url.Action(Model.AddLink.LinkPage.ActionName)" class="easyui-linkbutton" iconcls="icon-add" plain="true" id="append">添加分支</a>
            }
            else
            {
            <a href="#" class="easyui-linkbutton" iconcls="icon-add" plain="true" title="Add" id="append" >添加分支</a>
            }
            @if (Model.EditLink.BindingEnable)
            {
                <a href="@Url.Action(Model.EditLink.LinkPage.ActionName@*, new { autoKey=1}*@)" class="easyui-linkbutton" iconcls="icon-edit" plain="true" id="ToolbarEdit">编辑</a>
            }
            else
            {
                <a href="#" class="easyui-linkbutton" iconcls="icon-edit" plain="true">编辑</a>
            }
            
            @if (Model.SaveLink.BindingEnable)
            {
                <a href="Javascript:@Model.SaveLink.JavascriptFunName" class="easyui-linkbutton" iconcls="icon-save" plain="true">保存</a>
            }
            else
            {
                <a href="#" class="easyui-linkbutton" iconcls="icon-save" plain="true">保存</a>
            }
            @*@if (Model.IsSearchVisiable)
            {*@
                 
                @if (Model.FindLink.BindingEnable)
                {
                    using (Html.BeginForm(Model.FindLink.LinkPage.ActionName, Model.FindLink.LinkPage.ControllerName, FormMethod.Post, new { name = "frmFind" }))
                    {
                        <input name="keyword" />
                <input type="submit" />
                //<a href="@Url.Action(Model.SearchLink.LinkPage.ActionName, new { autoKey = Model.Parameters["Company_ID"] })" class="easyui-linkbutton" iconcls="icon-search" plain="true">查询</a>
                    }
                   
                }
                else
                {
                    <a href="#" class="easyui-linkbutton" iconcls="icon-search" plain="true">查询</a>
                }
            @*}*@
        </div>
}
