本文件记录配置工具及基本框架更新记录

WaitingList
1、PickBox没有等待测试
2、ComboTree没有等待测试
3、DataGrid初始数据等待测试；
4、用户属性等待测试；
5、邮件服务设置；
6、基本权限设置；
7、主从表数据存储问题；

Title:20141112
1、增加页面分组、页面、命令时会自动保存appconfig.xml;
2、保存页面时不再去保存appconfig.xml，也不会去保存事件配置；

Title:20141110
1、添加获取首字母的方法

Title:20140926
1、开始设计邮件及事件系统

TITLE:20140925
BUG
1、修改ComboBox生成页面代码在没有设定绑定值时少了结束符；
2、

NEW
1、将全局数据源增加了json树形式方法；
2、增加ComboTree的选择事件以及选中字段；

Title:20140924 更新记录
BUG
1、2	

NEW
1、初步完成table的表格排序方法；

Title:20140923 更新记录
BUG
1、修改Input控件没有根据类型创建对应对象的错误；
2、修改所有控件默认值不能更改的错误；

NEW
1、增加取参数时出错日志（写入log.txt文件）；
2、增加控件ComboTree；
3、增加控件ComboBox的url取参数；
4、增加编码的url访问（可以绑定ComboBox）；
5、增加SQL查询的url访问（可以绑定ComboBox)；
6、增加用户属性，在服务端可以通过系统参数方式获取；
7、修改按钮在设计时预览时，可以不判断权限直接生成html代码；
8、增加【获取常用描述】的url及值转换方法；
9、增加【Json数据树】转换，可以将列表数据转换为树形子列表json，可以直接绑定ComboTree；
10、增加DataGrid的数据源绑定，可以显示初始数据，界面已经更新；


Title:20140919 更新记录
BUG
1、修改Form绑定时没有处理列表，应该默认取列表第一行记录对象；
2、修改Action命令出错时，本身错误处理命令出错时死循环的BUG；
3、修改Action配置界面出错处理命令可以删除；
4、修改编辑页面的转换BUG，可以转换。

NEW
1、增加SQL查询命令执行结果属性Item，取Items的第一行，如果没有记录，则创建一个RowEntity，可以用[name]访问；
2、增加WEB测试命令，测试时用admin登录；
3、初步完成PickBox的设计，还未测试；
